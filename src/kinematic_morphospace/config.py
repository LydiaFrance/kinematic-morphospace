"""kinematic-morphospace configuration.

This module previously mutated sys.path at import time.
The package is now installed via ``uv`` (editable or regular install),
so runtime path manipulation is no longer needed.
"""